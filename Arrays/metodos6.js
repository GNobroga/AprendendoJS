`
    Métodos para o objeto Array

`;

const array1 = new Array(5); // Criar um Array através da função construtura. Alocando o tamanho especificado.

Array.of(1,2,3,4,5) // Cria um apartir de uma sequência de valores.

Array.from("Gabriel") // Cria um Array apartir de um iteravel