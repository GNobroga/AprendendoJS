`
    AJAX
        Asynchronous
        JavaScript
        and
        XML

    Ajax é só uma forma bonita de se referir ao processo de se comunicar com o servidor
    de forma assincrona transportando XML/Dados/JSON.

    Síncrono:

        Código é executado na ordem que aparece no arquivo.js.
        É bloqueante
    
    Assíncrono:

        Não precisa ser executado na mesma ordem que aparece no código.
        Não é bloqueante.
    
    XML:

        Arquivo que transporta dados.
    
    JSON:

        Arquivo que transporta dados.
        Tem que usar aspas duplas e não pode colocar vírgula no último elemento.

    HTPP (HyperText Transfer Protocol):

        Protocolo que permite a comunicação entre máquinas diferentes.
        Cliente >> Servidor
  
        Verbos HTTP (HyperText Transfer Protocol):


            Também conhecidos como métodos HTTP.
            São tipos de comunicação com o servidor.
            EX: acessar um página no browser (verbo GET).
            Enviar um formulário (verbo POST), etc.

            GET - Obter dados
            POST - Enviar dados (novo cadastro)
            PUT - Atualizar dados (substituir objeto completo)
            PATCH - Atualizar dados (substituir objeto parcial)
            DELETE - Excluir um registro no servidor
            OPTIONS - Permitir acesso de domínios diferentes

`;

