`

    Principais métodos:

        setTimeout(fn, ms) // Retorna um ID
        setInterval(fn, ms) // Retorna um ID
        clearTimeout(id) // Cancela o setTimeout
        clearInterval(id) // Cancela o setInterval


    O JavaScript é Single Thread:

        Um processo por vez é executado.

    O tempo colocado no setTimeout ou setInterval é o tempo no qual definimos para
    que ele entre na fila de execução e não que será executado naquele momento.
`;