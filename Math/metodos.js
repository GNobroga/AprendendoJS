`

    Principais métodos e propriedades da Classe Math:

        min 
        max
        round
        floor
        ceil
        pow
        sqrt
        cbrt
        random
`;


Math.min(1,2,3,4,5) // Retorna o menor valor
Math.max(1,2,3,4,5) // Retorna o maior valor

Math.round() // Serve para arredondar um número

Math.floor() // Para arrendodar para baixo

Math.ceil() // Para arrendodar para cima

Math.pow() // Retorna a potencia de um número

Math.sqrt() // Retorna a raiz quadrada

Math.cbrt() // Retorna a raiz cubica

Math.random() // Retorna número aleatório de 0 a 1