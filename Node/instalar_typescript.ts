`

    Instalar o TypeScript:

        npm install typescript -D

        npm install @types/node -D // Vai tornar o TypeScript mais inteligente.
    
    Iniciar arquivo de configuração TypeScript:

        tsc --init ou tsc init
    
    Organização das páginas:

        dist 
        src
    
    Configurar arquivo de configuração TypeScript:

        "modulResolution": "node"
        "rootDir": "./src"
        "outDir": "./dist"
    
    Monitorar arquivo TypeScript

        tsc -w

    @types/

        npm install @types/node - Ajuda o TypeScript a entender tipos que existem naquele ambiente.
    
`;