`

    NPM - Node Package Manager


    Iniciar um projeto do Node JS:

        npm init -y
            ou
        npm init

    Instalar um pacote:

        npm install package 

            flags:

                -g // Significa instalar globalmente no sistema operacional
                --save-dev ou -D // Coloca como dependência de desenvolvimento

        npm install // Vai instalar todas as dependencias que o projeto precisa.

    NPX:

        Executar NPX o NPM vai pesquisar o package no node modules e executa, caso não exista
        será instalado.

        É utilizado quando não instalamos o pacote como sendo global, mas sim local.
`;