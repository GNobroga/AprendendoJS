`

    DQL - Data Query Language

    COMANDOS: Select

    EX:

        SELECIONANDO TODOS OS DADOS DA TABELA:

            SELECT * FROM tabela;
        
        SELECIONANDO DADOS ESPECÍFICOS:

            SELECT atributo1, atributo2 FROM tabela;

        UTILIZANDO ALIAS (Apelido):

            SELECT t.atributo1 as apelido FROM tabela as t;
`;