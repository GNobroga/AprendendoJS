`


O que é ORM?

    ORM = Object Relational Mapper (Mapeamento objeto-relacional);

    A gente ensina o ORM como funciona o banco de dados e a partir disso
    ele sabe como manusear.

    Biblioteca ORM Sequelize.

        npm install --save saquelize

        npm install @types/sequelize
        npm install @types/node
        @types/validator
    
    Sequelize com Typescript

    Instalar biblioteca do Postgres:

        npm install pg pg-hstore

    Configurando variáveis de ambiente:

        PG_DB=postgres
        PG_USER=postgres
        PG_PASSWORD=1234
        PG_PORT=5432
`;

